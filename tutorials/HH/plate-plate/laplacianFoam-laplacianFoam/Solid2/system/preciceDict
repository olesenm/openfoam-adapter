FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      preciceDict;
}

preciceConfig "precice-config.xml";

participant Solid2;

modules (CHT);

interfaces
{
  Interface1
  {
    mesh              Solid2-Mesh;
    patches           (interface);
    
    readData
    (
      Heat-Flux
    );
    
    writeData
    (
      Temperature
    );
  };
};

CHT
{
   k   [ 1  1 -3 -1 0 0 0 ] 100;
   solverType "basic";
};